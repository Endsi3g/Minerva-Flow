# Minerva Flow — Design System

Le guide de référence complet (palette, typographie, catalogue de composants avec exemples de code) vit désormais dans un seul endroit pour éviter que deux copies ne divergent :

👉 **[`docs/DESIGN_SYSTEM.md`](docs/DESIGN_SYSTEM.md)**

Le catalogue vivant et interactif reste aussi accessible directement dans l'application :
👉 **`/[locale]/design-system`** (ex : `/fr/design-system`).
