# Implementation Tasks

- `[ ]` 1. Update upcoming shifts dates query (`lib/data/shift-schedules.ts`)
- `[ ]` 2. Update shift scheduled notifications recipient list (`app/(app)/horaire/actions.ts`)
- `[ ]` 3. Implement reservation status select and table type choices (`app/(app)/reservations/ReservationsView.tsx`)
- `[ ]` 4. Move Finance menu item to the main sidebar nav (`components/shell/AppSidebar.tsx`)
- `[ ]` 5. Update menu view category tabs and edit product modals (`app/(app)/menu/MenuView.tsx`)
- `[ ]` 6. Verify compilation and deploy to Vercel
